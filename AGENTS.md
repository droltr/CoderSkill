# CoderSkill Agent Instructions

For repository work, read `.agents/skills/professional-coding/SKILL.md` first. Use the smallest focused skill for narrow requests. Read `.coderskill/project.yml` when present. Write repository artifacts in English and communicate with the user in Turkish. Preserve `main`, use issue-linked topic branches, run validation, and never expose secrets or perform hardware writes.
